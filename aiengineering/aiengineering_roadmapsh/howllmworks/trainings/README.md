# TRAININGS

## install
```bash
pyenv versions
pyenv local 3.9.18
python --version
python3 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install numpy pandas datasets transformers torch torchvision Pillow scikit-learn optuna
deactivate
python3 main.py
```